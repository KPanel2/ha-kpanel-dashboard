{
  "config": {
    "step": {
      "user": {
        "title": "KPanel Dashboard",
        "description": "Select the Home Assistant user and dashboard path for KPanel kiosk auto-auth. Optional local base URL is used in hassTokens for LAN panels (e.g. http://172.16.24.1:8123).",
        "data": {
          "user_id": "Home Assistant user",
          "dashboard_path": "Dashboard path",
          "local_hass_url": "Local base URL for KPanel devices (optional)",
          "hide_header": "Hide header",
          "hide_sidebar": "Hide sidebar"
        },
        "data_description": {
          "local_hass_url": "Must match the URL your panels open (private IP/hostname). Leave blank to use HA Network internal URL, then external URL."
        }
      },
      "confirm": {
        "title": "Copy binding secret",
        "description": "Copy the binding secret below into the KPanel portal HA binding fields.\n\nUser: {username}\nDashboard: {dashboard_path}\nBinding secret: `{binding_secret}`",
        "data": {
          "binding_secret_display": "Binding secret (copy this)"
        }
      }
    },
    "error": {
      "unknown_user": "Selected user was not found or is inactive.",
      "invalid_path": "Dashboard path is required.",
      "invalid_local_url": "Local base URL must be an absolute http:// or https:// URL."
    },
    "abort": {
      "already_configured": "KPanel Dashboard is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "KPanel Dashboard options",
        "description": "Current binding secret: `{binding_secret}`\n\nPaste this into the KPanel portal as the HA binding secret. Optionally set a LAN base URL for panels.",
        "data": {
          "binding_secret_display": "Binding secret (copy this)",
          "local_hass_url": "Local base URL for KPanel devices",
          "rotate_binding_secret": "Generate a new binding secret (invalidates the old one)"
        },
        "data_description": {
          "local_hass_url": "Example: http://172.16.24.1:8123 — must match panel URLs. Leave blank to clear and use HA Network URLs."
        }
      },
      "show_secret": {
        "title": "New binding secret",
        "description": "Copy the new binding secret into KPanel now.\n\nBinding secret: `{binding_secret}`",
        "data": {
          "binding_secret_display": "New binding secret (copy this)"
        }
      }
    },
    "error": {
      "invalid_local_url": "Local base URL must be an absolute http:// or https:// URL."
    }
  }
}
